@model IEnumerable<WebApplication1.Models.Kelas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Zaman)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Makan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Zaman)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Makan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KelasID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KelasID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KelasID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
